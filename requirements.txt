numpy>=1.24
scipy>=1.11
pandas>=2.0
PyYAML>=6.0
torch>=2.0
matplotlib>=3.7
ortools>=9.7          # CP-SAT 精确求解（免授权）
visdom>=0.2           # 可选：训练实时曲线；服务未启动时自动降级
# gurobipy>=11.0      # 可选：有授权时自动启用，用于流体 LP 与 MILP 版精确解
